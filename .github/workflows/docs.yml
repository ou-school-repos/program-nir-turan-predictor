---
name: docs

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-pdfs:
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libimage-exiftool-perl=12.76+dfsg-1 \
            pandoc=3.1.3+ds-2 \
            qpdf=11.9.0-1.1build1 \
            python3-pip \
            texlive-bibtex-extra \
            texlive-fonts-recommended \
            texlive-latex-extra \
            texlive-lang-english \
            texlive-xetex
          python3 -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Set reproducible build epoch
        run: echo "SOURCE_DATE_EPOCH=$(git show -s --format=%ct HEAD)" >> "$GITHUB_ENV"

      - name: Build paper figures
        run: |
          mkdir -p docs/out
          python3 scripts/plot_18.py
          python3 scripts/plot_76.py --pdf
          python3 scripts/plot_anomaly.py

      - name: Build paper and markdown PDFs
        run: make docs paper

      - name: Compute PDF bundle name
        run: |
          short_sha="${GITHUB_SHA::7}"
          date_iso_built="$(date -u +%Y%m%d%H%M%S)"
          echo "PDF_BUNDLE_DIR=all-pdfs_${short_sha}_${date_iso_built}_${GITHUB_RUN_ID}_${GITHUB_RUN_ATTEMPT}" >> "$GITHUB_ENV"

      - name: Stage PDFs for upload
        run: |
          mkdir -p "$PDF_BUNDLE_DIR"
          mapfile -t pdfs < <(find . -type f -name '*.pdf' \
            -not -path './.git/*' \
            -not -path './.github/*' \
            -not -path './.lake/*' \
            -not -path './.tmp/*' \
            -not -path "./$PDF_BUNDLE_DIR/*" \
            | sort)
          for pdf in "${pdfs[@]}"; do
            rel=${pdf#./}
            mkdir -p "$PDF_BUNDLE_DIR/$(dirname "$rel")"
            cp "$pdf" "$PDF_BUNDLE_DIR/$rel"
          done

      - name: Upload all-pdfs
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.PDF_BUNDLE_DIR }}
          path: ${{ env.PDF_BUNDLE_DIR }}
          if-no-files-found: error
